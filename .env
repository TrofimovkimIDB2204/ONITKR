# База данных
POSTGRES_USER=postgres
POSTGRES_PASSWORD=94227
POSTGRES_DB=simracing_db
DB_HOST=db
DB_PORT=5432

# Приложение
APP_PORT=8000

# Формируем URL динамически (многие библиотеки это поддерживают, 
# либо можно собирать строку прямо в коде Python через os.getenv)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}